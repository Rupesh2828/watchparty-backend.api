import prisma from "../config/database.js";
import { Request, Response } from "express";

export const getAllMediaLibraryVideos = async(req:Request, res:Response): Promise<void> => {
   
     try {
        //Fetch all video content in the library.
                
        
     } catch (error) {
        
     }

}